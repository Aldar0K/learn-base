# Database Design

Описание архитектуры базы данных LearnBase.

## Обзор

Минимальная архитектура БД для MVP клона Stepik. Фокус на простоте и скорости разработки.

## Сущности

### 1. User (Пользователь)
Базовые пользователи системы без профилей и соцсетей.

### 2. Course (Курс)
Обучающий курс, созданный автором.

### 3. Lesson (Урок)
Урок внутри курса, имеет позицию для упорядочивания.

### 4. Step (Шаг)
Ключевая сущность - шаг урока. Может быть викториной, текстом или кодом.

### 5. Enrollment (Запись на курс)
Связь пользователя с курсом (многие-ко-многим).

### 6. Submission (Ответ)
Ответ пользователя на шаг урока.

## Схема связей (High-level)

```
User
 └── Enrollment ── Course
                     └── Lesson
                           └── Step
                                  └── Submission ── User
```

## Детальное описание таблиц

### Users

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Первичный ключ |
| `email` | VARCHAR | Email (уникальный) |
| `name` | VARCHAR | Имя пользователя (опционально) |
| `password_hash` | VARCHAR | Хеш пароля |
| `role` | ENUM | Роль: `student`, `author`, `admin` |
| `created_at` | TIMESTAMP | Дата создания |

**Особенности:**
- Без профилей, соцсетей и дополнительных полей
- Минимальная структура для MVP

### Courses

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Первичный ключ |
| `title` | VARCHAR | Название курса |
| `description` | TEXT | Описание курса |
| `author_id` | UUID FK | Ссылка на `users(id)` |
| `is_published` | BOOLEAN | Опубликован ли курс |
| `created_at` | TIMESTAMP | Дата создания |

**Связи:**
- `author_id` → `users(id)` (один автор может создать много курсов)

### Lessons

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Первичный ключ |
| `course_id` | UUID FK | Ссылка на `courses(id)` |
| `title` | VARCHAR | Название урока |
| `position` | INT | Порядок внутри курса |

**Связи:**
- `course_id` → `courses(id)` (один курс содержит много уроков)

**Индексы:**
- `idx_lessons_course` на `course_id`

### Steps (Ключевая сущность)

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Первичный ключ |
| `lesson_id` | UUID FK | Ссылка на `lessons(id)` |
| `type` | ENUM | Тип: `quiz`, `text`, `code` |
| `content` | JSONB | Содержимое шага (зависит от типа) |
| `position` | INT | Порядок внутри урока |

**Связи:**
- `lesson_id` → `lessons(id)` (один урок содержит много шагов)

**Примеры `content`:**

**Quiz (викторина):**
```json
{
  "question": "2 + 2 = ?",
  "options": [2, 3, 4],
  "correct": [4]
}
```

**Code (код):**
```json
{
  "language": "python",
  "tests": [
    { "input": "2", "output": "4" }
  ]
}
```

**Text (текст):**
```json
{
  "text": "Объяснение материала..."
}
```

**Индексы:**
- `idx_steps_lesson` на `lesson_id`

### Enrollments (User ↔ Course)

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Первичный ключ |
| `user_id` | UUID FK | Ссылка на `users(id)` |
| `course_id` | UUID FK | Ссылка на `courses(id)` |
| `created_at` | TIMESTAMP | Дата записи |

**Связи:**
- `user_id` → `users(id)` (один пользователь может быть записан на много курсов)
- `course_id` → `courses(id)` (один курс может иметь много записей)

**Ограничения:**
- `UNIQUE(user_id, course_id)` - пользователь не может быть записан на курс дважды

### Submissions (Ответы)

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Первичный ключ |
| `user_id` | UUID FK | Ссылка на `users(id)` |
| `step_id` | UUID FK | Ссылка на `steps(id)` |
| `answer` | JSONB | Ответ пользователя |
| `is_correct` | BOOLEAN | Правильность ответа |
| `created_at` | TIMESTAMP | Дата ответа |

**Связи:**
- `user_id` → `users(id)` (один пользователь может дать много ответов)
- `step_id` → `steps(id)` (один шаг может иметь много ответов)

**Примеры `answer`:**

**Quiz:**
```json
{ "choice": 4 }
```

**Text:**
```json
{ "text": "my explanation" }
```

**Code:**
```json
{ "code": "print(2 + 2)" }
```

**Индексы:**
- `idx_submissions_user_step` на `(user_id, step_id)`

## Индексы

Минимальные индексы для оптимизации запросов:

```sql
CREATE INDEX idx_lessons_course ON lessons(course_id);
CREATE INDEX idx_steps_lesson ON steps(lesson_id);
CREATE INDEX idx_submissions_user_step ON submissions(user_id, step_id);
```

## Обоснование архитектуры

### ✅ Плюсы

- **Очень быстрый старт** - минимум таблиц
- **Простые JOIN'ы** - понятная структура связей
- **Отлично ложится на REST / GraphQL** - простая модель данных
- **Легко писать migrations** - простая структура

### ❌ Минусы (осознанные для MVP)

- **JSONB сложнее валидировать** - валидация на уровне приложения
- **Нет нормализации шагов** - разные типы в одной таблице
- **Нет прогресса** - не отслеживается прогресс прохождения
- **Нет попыток (attempts)** - только последний ответ
- **Нет версий шагов** - нельзя редактировать без потери истории

👉 **Это нормально для MVP** - можно добавить позже при необходимости.

## Миграции

Миграции находятся в `backend/prisma/migrations/`

### Алгоритм работы с миграциями в Docker окружении

**Важно**: Все команды Prisma выполняются внутри Docker контейнера, где есть доступ к базе данных через `DATABASE_URL`.

#### 1. Изменить схему Prisma

Редактируем `backend/prisma/schema.prisma`:
```prisma
model User {
  id            String    @id @default(uuid())
  email         String    @unique
  name          String?   // ← Добавляем новое поле
  // ...
}
```

#### 2. Создать и применить миграцию

Выполняем в Docker контейнере:
```bash
cd learnbase-config-dev
docker compose exec backend npx prisma migrate dev --name migration_name
```

**Что происходит:**
- Prisma сравнивает схему с текущей БД
- Создает SQL-файл миграции в `backend/prisma/migrations/YYYYMMDDHHMMSS_migration_name/migration.sql`
- Применяет миграцию к БД
- Автоматически генерирует обновленный Prisma Client

#### 3. Проверить статус миграций

```bash
docker compose exec backend npx prisma migrate status
```

#### Команды для работы с миграциями

```bash
# Создать и применить миграцию (для разработки)
docker compose exec backend npx prisma migrate dev --name migration_name

# Применить существующие миграции (для продакшена)
docker compose exec backend npx prisma migrate deploy

# Проверить статус миграций
docker compose exec backend npx prisma migrate status

# Сгенерировать Prisma Client (обычно делается автоматически)
docker compose exec backend npx prisma generate

# Откатить последнюю миграцию (только в dev, сбросит БД)
docker compose exec backend npx prisma migrate reset
```

**Важные моменты:**
- Миграции выполняются в контейнере, где есть доступ к БД через `DATABASE_URL`
- Файлы миграций создаются в `backend/prisma/migrations/` и должны быть в git
- Prisma Client генерируется автоматически после применения миграции
- В продакшене используйте `prisma migrate deploy` вместо `prisma migrate dev`

## Prisma Studio

Для визуализации и редактирования данных используйте Prisma Studio:

```bash
# Dev среда (отдельный контейнер)
cd learnbase-config-dev
docker compose up prisma-studio

# Или запустить все сервисы включая Prisma Studio
docker compose --env-file .env up --build
```

Prisma Studio будет доступен на **http://localhost:5555**

**Примечание**: Prisma Studio доступен только в dev окружении. В production он не запускается.
